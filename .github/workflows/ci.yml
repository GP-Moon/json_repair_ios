name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    name: Swift Build and Test
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build
      - name: Test
        run: swift test
